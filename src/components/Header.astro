<header>
    <div class="header-inner">
        <a href="/">
            <img src="/lotus-icon.webp" alt="lotus sur l'eau">
        </a>
    <h1>
        Hypnose Malika
    </h1>
    <button id="burger" aria-label="Menu">
    <span></span>
    <span></span>
    <span></span>
    </button>
    <nav id="nav-menu" class="nav-menu">
        <ul>
            <li><a href="/#about">Mon chemin de vie</a></li>
            <li><a href="/#intervention">Mes domaines d'intervention</a></li>
            <li><a href="/#seance">Déroulement d'une séance</a></li>
            <li><a href="/#contact">Prendre RDV</a></li>
            <li><a href="/#FAQ">Foire aux questions</a></li>
        </ul>
    </nav>
    </div>
  </header>

  <style>
    
    header {
        position: fixed;
        top: 0;
        left: 0;
        width: 100%;
        height: 100px;
        background-color: var(--color-dark-blue);
        color: var(--color-white);
        box-shadow: 0 4px 6px rgba(0,0,0,0.1);
        z-index: 1000;
        display: flex;
        justify-content: center;
        align-items: center;
    }

    .header-inner {
        width: 100%;
        max-width: 1200px;
        display: flex;
        justify-content: space-between;
        align-items: center;
        padding: 0 1rem;
    }

    header img {
        width: 60px;
        height: 60px;
        border-radius: 100%;
    }

    header h1 {
        font-size: 1.75rem;
        flex-shrink: 0;
    }

    /* hamburger menu */

    .nav-menu {
        position: fixed;
        top: 100px;
        right: -100%;
        width: 50%;
        height: calc(100vh - 100px);
        background-color: var(--color-dark-blue);
        border-left: 4px solid var(--color-sand);
        box-shadow: -4px 0 20px rgba(0,0,0,0.3);
        border-radius: 0 0 0 12px;
        transition: right 0.4s cubic-bezier(0.77, 0, 0.175, 1);
        z-index: 999;
    }

    .nav-menu.open {
        right: 0;
    }

    .nav-menu ul {
        list-style: none;
        padding: 2rem;
        display: flex;
        flex-direction: column;
        gap: 1.5rem;
    }

    .nav-menu li {
        margin-bottom: 1.5rem;
        text-align: center;
    }

    .nav-menu a {
        color: var(--color-white);
        text-decoration: none;
        font-weight: bold;
        font-size: 1.2rem;
        transition: color 0.3s, transform 0.3s;
    }

    .nav-menu a:hover {
        color: var(--color-sand);
        transform: translateX(5px);
    }

    #burger {
        display: flex;
        flex-direction: column;
        justify-content: space-between;
        width: 24px;
        height: 18px;
        background: none;
        border: none;
        cursor: pointer;
        padding: 0;
    }

    #burger span {
        display: block;
        height: 3px;
        width: 100%;
        background-color: var(--color-white);
        border-radius: 2px;
    }

    /* TABLETTE / DESKTOP */
    header {
  position: fixed;
  top: 0;
  left: 0;
  width: 100%;
  background-color: var(--color-dark-blue);
  z-index: 1000; /* s'assurer qu'il reste au-dessus */
  box-shadow: 0 4px 6px rgba(0,0,0,0.1);
}

.header-inner {
  max-width: 1200px;
  margin: 0 auto;
  padding: 0 1rem;
  display: flex;
  justify-content: space-between;
  align-items: center;
  height: 100px; /* mobile par défaut */
  position: relative;
}

/* logo */
header img {
  width: 60px;
  height: 60px;
  border-radius: 50%;
}

/* titre */
header h1 {
  font-size: 1.5rem;
  flex-shrink: 0;
  text-align: center;
}

/* burger menu mobile */
#burger {
  display: flex;
  flex-direction: column;
  justify-content: space-between;
  width: 24px;
  height: 18px;
  background: none;
  border: none;
  cursor: pointer;
  padding: 0;
}

#burger span {
  display: block;
  height: 3px;
  width: 100%;
  background-color: var(--color-white);
  border-radius: 2px;
}

/* nav mobile */
.nav-menu {
  position: fixed;
  top: 100px;
  right: -100%;
  width: 50%;
  height: calc(100vh - 100px);
  background-color: var(--color-dark-blue);
  transition: right 0.4s ease;
  z-index: 999;
}

.nav-menu.open {
  right: 0;
}

.nav-menu ul {
  list-style: none;
  padding: 2rem;
  display: flex;
  flex-direction: column;
  gap: 1rem;
}

/* --- Desktop --- */
@media (min-width: 1024px) {

    header {
        height: 200px;
    }
  .header-inner {
    flex-direction: column;
    align-items: center;
    height: 160px;
  }

  header img {
    position: absolute; /* logo reste à gauche */
    left: 0;
    top: 50%;
    transform: translateY(-50%);
    width: 100px;
    height: 100px;
  }

  header h1 {
    text-align: center;
    font-size: 3rem;
    margin-top: 0.5rem;
  }

  #burger {
    display: none;
  }

  .nav-menu {
    position: static; /* nav dans le flux */
    width: auto;
    height: auto;
    display: flex;
    justify-content: center;
    margin-top: 0.5rem;
    box-shadow: none;
    border-left: none;
  }

  .nav-menu ul {
    flex-direction: row;
    gap: 2rem;
    padding: 0;
  }

  .nav-menu li {
    margin: 0;
  }

  .nav-menu a {
    font-size: 1.1rem;
    font-weight: 500;
    color: var(--color-white);
  }

  .nav-menu a:hover {
    color: var(--color-sand);
  }
}


  </style>
  

  <script>
    const burger = document.getElementById('burger')
    const menu = document.getElementById('nav-menu');

    burger?.addEventListener('click', ()=> {
        menu?.classList.toggle('open');
    })

    menu?.querySelectorAll('a').forEach(link => {
        link.addEventListener('click', ()=> {
            menu.classList.remove('open');
        })
    })
  </script>